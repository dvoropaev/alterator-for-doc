#ifndef VERSION_H
#define VERSION_H

#define ALTERATORCTL_VERSION_MAJOR ${ALTERATORCTL_VERSION_MAJOR}
#define ALTERATORCTL_VERSION_MINOR ${ALTERATORCTL_VERSION_MINOR}
#define ALTERATORCTL_VERSION_PATCH ${ALTERATORCTL_VERSION_PATCH}

#define STRINGIFY(x) #x
#define TOSTRING(x) STRINGIFY(x)

#define ALTERATORCTL_VERSION TOSTRING(ALTERATORCTL_VERSION_MAJOR) "." \
                             TOSTRING(ALTERATORCTL_VERSION_MINOR) "." \
                             TOSTRING(ALTERATORCTL_VERSION_PATCH)

#endif // VERSION_H
